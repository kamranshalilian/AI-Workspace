{
  "name": "ai-workspace",
  "version": "0.1.0",
  "description": "Local-first, Git-friendly, tool-agnostic AI context and workspace layer",
  "type": "module",
  "bin": {
    "aiw": "./dist/cli/index.js"
  },
  "exports": {
    ".": "./dist/core/index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "files": [
    "dist",
    "definitions",
    "LICENSE",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json && node scripts/copy-definitions.mjs",
    "clean": "node -e \"const fs=require('fs'); for (const d of ['dist','dist-test']) fs.rmSync(d,{recursive:true,force:true})\"",
    "test": "npm run build && tsc -p tsconfig.test.json && node scripts/run-tests.mjs",
    "aiw": "node dist/cli/index.js"
  },
  "keywords": [
    "ai",
    "workspace",
    "context",
    "cli"
  ],
  "license": "MIT",
  "dependencies": {
    "minimatch": "^10.0.3",
    "yaml": "^2.8.1"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2"
  }
}
